import { NoMatch } from 'react-shop-mui/NoMatch';

export default function NotFoundCheckoutPage() {
  return (
    <NoMatch message="It looks like the cart ID you're trying to access is invalid or no longer exists. Please return to the shop and add items to your cart again." />
  );
}
